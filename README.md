# Cursor IDE Setup — Portfolio Project

## Tools Installed
- Cursor IDE (https://cursor.com)
- Claude Code extension (via Cursor Extensions)
- Codex extension (via Cursor Extensions)

## Steps Completed
1. Installed Cursor IDE
2. Searched for and installed Claude Code extension
3. Searched for and installed Codex extension
4. Created a public GitHub repository
5. Cloned the repository and opened it in Cursor

## Issues I Ran Into & How I Solved Them
- Ran `git clone` with angle brackets around the URL — removed them and it worked
- Got an `xcrun` error due to missing Xcode Command Line Tools — resolved by running `xcode-select --install`
- Initially looked for extensions on the Cursor website instead of inside the app
